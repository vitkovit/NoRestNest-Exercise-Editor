# Dependencies (reproducible from package-lock.json — run `npm ci`)
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db

# Backup files
*.bk

# Tooling / local
.claude/
assets/

# NOTE: .editor-state.json is intentionally TRACKED.
# It holds the shared list of edited exercise IDs that we
# coordinate between editing sessions. Do NOT ignore it.
